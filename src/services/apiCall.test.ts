import { apiCall } from "./apiCall";

describe('getData', () => {
    // let response;
    // beforeEach(() => {
    //     response = {
    //         meta: {
    //             count: 10, page_total: 78, total: 780, limit: 10, page: 1
    //         },
    //         status: "ok"
    //     }
    // })
    test('get tanks list', () => {
        expect('a').toBe('a');
    });
})
